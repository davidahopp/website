%nav.top-bar{"data-topbar" => ""}
  %ul.title-area
    %li.name
      %h1
        %a{href: root_path} davidahopp

  = render partial: 'home/top_bar_section', locals: {title: 'contact info', path: contact_index_path}