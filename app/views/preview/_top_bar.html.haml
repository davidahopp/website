%nav.top-bar{"data-topbar" => ""}
  %ul.title-area
    %li.name
      %h1
        %a{href: root_path} davidahopp

  = render partial: 'preview/top_bar_section', locals: {title: 'home', path: preview_index_path}
  = render partial: 'preview/top_bar_section', locals: {title: 'contact info', path: preview_contact_index_path}